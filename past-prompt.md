at every place you've used the color #262626 or tailwind classes such as red-500, use variables instead as per tailwind CSS conventions. Remove static colors from every place you find and replace them with the variable

- Define the full color set first as per project requirements then at every place you'll be using those variables instead of hardcoded colors or tailwind classes.